export enum Status {
  PENDING = 'pending',
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
}
export enum UserRoles {
  RWX_ADMIN = 'admin',
  RWX_LISTENER = 'listener',
  RWX_DISTRIBUTOR = 'distributor',
}
